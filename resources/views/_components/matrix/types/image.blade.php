{{--
  outset left: left outside container, text floating around
  inset center: image width smaller than container, centered
  outset center: image width slightly larger than container, "popping out" left and right of the container
  fill width: image width equals window width
--}}
@include("_components.matrix.types.image_$type")
